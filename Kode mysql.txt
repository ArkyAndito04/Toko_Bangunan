CREATE DATABASE toko_bangunan;
USE toko_bangunan;

CREATE TABLE pelanggan (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nama_lengkap VARCHAR(100),
    nomor_hp VARCHAR(15) UNIQUE,
    alamat TEXT,
    password VARCHAR(255)
);

CREATE TABLE admin (
    id_admin INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    password VARCHAR(255)
);

-- Admin Default: admin / admin123
INSERT INTO admin (username, password) VALUES 
('admin', 'scrypt:32768:8:1$vXy... (Hash dibuat di app.py)');

CREATE TABLE produk (
    id_produk INT AUTO_INCREMENT PRIMARY KEY,
    nama_produk VARCHAR(100),
    harga INT,
    stok INT,
    gambar VARCHAR(255) DEFAULT 'default.jpg'
);

CREATE TABLE pesanan (
    id_pesanan INT AUTO_INCREMENT PRIMARY KEY,
    id_pelanggan INT,
    total_bayar INT,
    status VARCHAR(50) DEFAULT 'Diproses',
    notif_viewed TINYINT(1) DEFAULT 0,
    tanggal_pesanan TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (id_pelanggan) REFERENCES pelanggan(id)
);

UPDATE produk SET stok = 0 WHERE stok IS NULL;

UPDATE pesanan SET notif_viewed = 1;

 CREATE TABLE detail_pesanan (
    id_detail INT AUTO_INCREMENT PRIMARY KEY,
    id_pesanan INT,
    id_produk INT,
    jumlah INT,
    harga_satuan INT,
    FOREIGN KEY (id_pesanan) REFERENCES pesanan(id_pesanan) ON DELETE CASCADE,
    FOREIGN KEY (id_produk) REFERENCES produk(id_produk)
    );

ALTER TABLE produk ADD COLUMN status VARCHAR(20) DEFAULT 'aktif';